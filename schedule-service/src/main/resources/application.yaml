server:
  port: 8083
  servlet:
    context-path: /schedules
spring:
  application:
    name: schedule-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/schedule-service?authSource=admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true